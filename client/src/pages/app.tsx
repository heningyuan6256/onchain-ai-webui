import { FC } from "react";

export interface AppChat {

}

const AppChat: FC<AppChat> = (props) => {
    return <div className="flex justify-center items-center h-full w-full">
        AppChat
    </div>
}

export default AppChat